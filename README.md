# Sequelize Query Builder for NestJS

## Features

- Dynamic SELECT, WHERE, JOIN, ORDER BY, GROUP BY
- Supports aliases, pagination, and more.

## Installation

```bash
npm install sequelize-query-builder
```
